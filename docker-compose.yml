services:
  cloud:
    container_name: cloud
    image: ghcr.io/jakeoverall/simple_server:latest
    ports:
      - "3000:8080"
    env_file:
      - .env.cloud-infrastructure-lab
    labels:
      caddy.domain: cloud.dallinhooper.com
